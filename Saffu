A = int(input('Enter any number:'))
prime = True 
#just assuming

for i in range(2,A): 
    #A/2 samma pugxa from 2
    if A % i == 0:
        prime = False
        break

if prime:
    print('The number is prime')

else:
    print('The number is not prime')  
